# PrestaShop docker template

A simple Prestashop

Basically, it creates and runs and new prestashop

## NOT PRODUCTION READY

Some changes to make :

database environment in docker-compose.yml and ServerName in ./docker/apache/Dockerfile
